function toggleMode() {
  const html = document.documentElement

  // if(html.classList.contains('light')) {
  //  html.classList.remove('light')
  //} else {
  // html.classList.add('light')
  //} 

  //o toggle faz a mesma função q o codigo acima

  html.classList.toggle('light')
}